# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_Build_and_Deploy_(C#_Nuke) --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: Build_and_Deploy_(C#_Nuke)

on:
  push:
    branches:
      - master

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v4
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: '
        run: ./build.cmd 
        env:
          DockerHubPassword: ${{ secrets.DOCKER_HUB_PASSWORD }}
          DockerHubUsername: ${{ secrets.DOCKER_HUB_USERNAME }}
          LandsatApiRenderDeployHook: ${{ secrets.LANDSAT_API_RENDER_DEPLOY_HOOK }}
          LandsatUiRenderDeployHook: ${{ secrets.LANDSAT_UI_RENDER_DEPLOY_HOOK }}
